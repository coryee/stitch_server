

class SSPool(object):

    def __init__(self):
        super(SSMaster, self).__init__()
        self._list = []

    def add_item(item):
        self._list.append(item)
        return

    def remove_item(item):
        self._list.remove(item)
        return
